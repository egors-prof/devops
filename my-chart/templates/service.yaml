{{- if .Values.createService }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - protocol: TCP
      port: 8000
      targetPort: {{ .Values.port }}
{{- end }}